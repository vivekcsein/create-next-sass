import React from "react";
import "../../styles/content/Hero.scss";
const Hero = () => {
  return (
    <div className="fullscreen bg_tricolor Hero">
      A NextJS starter template to create full-stack production level
      web-applications.
    </div>
  );
};

export default Hero;
